export function canDump(selectedItem) {
    // Need to check condition that player has product to dump

    // How many can be dumped?
    const inventoryAmount = selectedItem.invQty
    
    return inventoryAmount
}